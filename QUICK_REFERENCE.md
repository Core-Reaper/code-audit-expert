# Code Audit Expert v2.0 - 10大维度速查卡

## 🎯 审计维度总览

| 维度 | 名称 | 严重等级 | 核心关注点 | 检查点数 |
|------|------|---------|-----------|---------|
| **3.1** | 自动化流程 & 路由跳转 | 🔴 致命/高危 | 定时器管理、流程控制、数据透传 | 10 |
| **3.2** | 用户业务操作行为 | 🟠 高危/中危 | 增删改查、批量操作、防重放 | 10 |
| **3.3** | 前端列表 & 表格渲染 | 🟡 中危/低危 | 懒加载、分页、缓存、虚拟列表 | 10 |
| **3.4** | 全局统计 & 聚合数据 | 🟡 中危/低危 | 统计准确性、缓存策略、权限可见 | 10 |
| **3.5** | 交互提示 & 消息反馈 | 🔵 低危/优化 | Toast、二次确认、脱敏、多语言 | 10 |
| **3.6** | 前后端通信 & 接口交互 | 🟠 高危/中危 | 参数校验、重试机制、分片上传 | 10 |
| **3.7** | 异常容错 & 边界场景 | 🟠 高危/中危 | 网络故障、超时、并发、兼容性 | 10 |
| **3.8** | 权限控制 & 访问安全 | 🔴 致命/高危 | 越权访问、按钮显隐、路由守卫 | 10 |
| **3.9** | 缓存存储 & 状态管理 | 🟡 中危/低危 | LocalStorage、Token、多标签页 | 10 |
| **3.10** | 业务流程闭环 & 全链路 | 🟠 高危/中危 | 状态机、断点续行、日志溯源 | 10 |

**总计**: 10 个维度 × 10 个检查点 = **100 个精细化检查点**

---

## 🔍 各维度核心检查清单

### 3.1 自动化流程 & 路由跳转

```
✅ 文件上传后是否自动触发后续任务？
✅ setTimeout/setInterval 是否在组件卸载时清理？
✅ 串行任务是否严格按顺序执行？
✅ 路由跳转前是否校验登录状态和权限？
✅ 用户中断流程后是否有恢复机制？
✅ 后台静默任务是否阻塞主线程？
✅ 跳转前数据预加载是否完整？
✅ 跨页面数据传递是否经过校验？
✅ 关键步骤是否可被用户跳过？
✅ 超时后资源是否正确释放？
```

**典型漏洞**: 定时器未清理导致内存泄漏、路由跳转无权限校验

---

### 3.2 用户业务操作行为

```
✅ 取消操作是否真正停止后端任务？
✅ 批量删除是否有数量限制和权限校验？
✅ 刷新后数据是否正确重新加载？
✅ 前端参数校验是否配合后端二次校验？
✅ 表单提交后按钮是否禁用防止重复提交？
✅ 删除操作是否校验数据所有权？
✅ 撤销操作的范围和限制是否明确？
✅ 草稿自动保存的频率和时机是否合理？
✅ 撤销失败是否有错误提示和补救途径？
✅ 大批量操作是否分批处理并支持熔断？
```

**典型漏洞**: 批量删除无事务控制、重复提交无防重放机制

---

### 3.3 前端列表 & 表格渲染

```
✅ 刷新是否重新请求数据还是使用缓存？
✅ 滚动加载是否有防抖和 loading 状态？
✅ 切换分页后总数是否更新？
✅ 进度数据来源是否可靠（轮询/WebSocket）？
✅ 状态值与显示文本的映射是否正确？
✅ 局部刷新与全局刷新策略是否合理？
✅ 空列表是否有友好提示？
✅ 排序/筛选是否触发重新请求？
✅ 缓存过期策略是否合理？
✅ 虚拟列表高度计算是否准确？
```

**典型漏洞**: 快速滚动触发重复请求、缓存脏数据残留

---

### 3.4 全局统计 & 聚合数据

```
✅ 统计数据是实时计算还是定时更新？
✅ 数据变更后统计是否同步更新？
✅ 跨模块数据聚合是否一致？
✅ 统计口径定义是否清晰？
✅ 零值/负值/异常值是否有兜底显示？
✅ 大盘数据更新频率是否合理？
✅ 缓存 TTL 设置是否合理？
✅ 子页面操作是否通知父页面更新统计？
✅ 离线时统计数据是否本地缓存？
✅ 不同角色看到的统计数据是否不同？
```

**典型漏洞**: 每次访问都全表扫描统计、缓存未及时失效

---

### 3.5 交互提示 & 消息反馈

```
✅ 所有用户操作是否有明确反馈？
✅ 刷新完成后是否有提示？
✅ Toast/Modal/Notification 层级是否合理？
✅ 高危操作是否有二次确认？
✅ 是否存在无提示就执行的高危操作？
✅ Toast 自动消失时间是否合理（3-5秒）？
✅ 错误提示是否说明具体原因？
✅ 批量操作是否有进度展示？
✅ 不同环境的提示文案是否区分？
✅ 提示信息中敏感信息是否脱敏？
```

**典型漏洞**: 删除成功仅显示"成功"未说明删除内容、敏感信息未脱敏

---

### 3.6 前后端通信 & 接口交互

```
✅ 前端参数名/类型是否与后端期望一致？
✅ 前端删除后后端是否真正删除数据？
✅ 乐观更新 vs 等待服务端响应的选择是否合理？
✅ 缓存过期策略和数据变更失效机制？
✅ 失败后是否自动重试？重试策略？
✅ 前端校验是否可被绕过？后端二次校验？
✅ 响应格式不符合预期时的容错处理？
✅ CORS 配置是否合理？代理转发安全性？
✅ API 签名算法和 Token 刷新机制？
✅ 大文件是否分片上传支持断点续传？
```

**典型漏洞**: 大文件直接上传无分片、前端校验可被绕过

---

### 3.7 异常容错 & 边界场景

```
✅ 网络断开时的错误提示和重试策略？
✅ 长时间无响应任务的看门狗监控？
✅ 空文件/超大文件的上传风控？
✅ 页面强制关闭时未保存数据的提醒？
✅ 500 错误的友好提示和服务降级？
✅ 不支持的 API 是否有 polyfill？
✅ 并发修改同一数据的冲突解决？
✅ 未授权路由的重定向和拦截？
✅ Token 过期的检测和自动刷新？
✅ 多标签页登录不同账号的数据隔离？
```

**典型漏洞**: 无超时设置导致请求永久挂起、并发修改无锁保护

---

### 3.8 权限控制 & 访问安全

```
✅ 按钮是否根据用户角色动态显示/隐藏？
✅ 直接调用 API 是否绕过前端权限检查？
✅ API 接口的权限校验是否完整？
✅ 不同操作是否有不同的权限要求？
✅ 操作日志的可见范围和脱敏？
✅ 管理端接口是否对用户端开放？
✅ 临时权限的有效期和自动回收？
✅ 权限变更后的缓存失效机制？
✅ 路由守卫配置是否完整？
✅ 列表数据是否按权限过滤？
```

**典型漏洞**: 列出所有用户接口无权限校验、前端隐藏按钮但后端无校验

---

### 3.9 缓存存储 & 状态管理

```
✅ LocalStorage 存储的数据敏感性和容量？
✅ 页面卸载时状态是否清理？
✅ Token 有效期和 Refresh Token 轮换？
✅ 本地操作历史记录的脱敏？
✅ LocalStorage 数据是否可被篡改？
✅ 表单数据刷新后是否保留？
✅ 多标签页登录同一账号的状态同步？
✅ Service Worker 缓存的安全性？
✅ 本地日志的内容范围和隐私保护？
✅ 主动清理缓存的入口和条件？
```

**典型漏洞**: 用户信息明文存储在 LocalStorage、XSS 可直接窃取

---

### 3.10 业务流程闭环 & 全链路

```
✅ 每个环节的状态流转和失败回滚？
✅ 状态机设计是否完整？非法状态转换拦截？
✅ 中断后能否从断点继续？
✅ 关键操作是否有完整日志？
✅ 流程中是否有遗漏的节点？
✅ 模块间的依赖关系和接口契约？
✅ 旧数据迁移和 API 版本兼容？
✅ 测试环境和生产环境流程差异？
✅ 是否符合行业规范和法律法规？
✅ 漏洞在整个链路中的影响范围？
```

**典型漏洞**: 流程失败无回滚导致脏数据、无状态记录无法追溯

---

## 🚀 快速审计流程

### 第一步：确定审计范围
```
□ 单文件审计 → 重点关注 3.1/3.6/3.8
□ 模块审计 → 覆盖全部 10 维度
□ 全项目审计 → 10 维度 + 逐行代码分析
```

### 第二步：执行 10 维度检查
```
□ 3.1 自动化流程 - 检查定时器、路由、流程控制
□ 3.2 用户操作 - 检查增删改查、批量操作
□ 3.3 列表渲染 - 检查懒加载、分页、缓存
□ 3.4 统计数据 - 检查准确性、缓存、权限
□ 3.5 交互提示 - 检查反馈、脱敏、多语言
□ 3.6 前后端通信 - 检查参数、重试、分片
□ 3.7 异常容错 - 检查超时、并发、兼容性
□ 3.8 权限控制 - 检查越权、按钮显隐、路由
□ 3.9 缓存状态 - 检查 LocalStorage、Token
□ 3.10 业务闭环 - 检查状态机、日志、全链路
```

### 第三步：问题定级与修复
```
🔴 致命/高危 → 立即修复（24小时内）
🟠 中危 → 优先修复（1周内）
🟡 低危/优化 → 逐步优化（1个月内）
```

---

## 📊 常见问题分布

根据历史审计数据统计：

| 维度 | 问题占比 | 高频问题 |
|------|---------|---------|
| 3.8 权限控制 | 25% | 越权访问、按钮无后端校验 |
| 3.6 前后端通信 | 20% | 参数校验缺失、无重试机制 |
| 3.7 异常容错 | 18% | 超时未设置、并发冲突 |
| 3.1 自动化流程 | 12% | 定时器未清理、流程无回滚 |
| 3.2 用户操作 | 10% | 批量操作无事务、重复提交 |
| 3.9 缓存状态 | 8% | 敏感信息明文存储 |
| 3.10 业务闭环 | 4% | 状态机不完整、日志缺失 |
| 3.3/3.4/3.5 | 3% | 列表性能、统计缓存、提示脱敏 |

---

## 💡 审计技巧

### 高效发现问题的方法

1. **逆向思维**: 假设每个功能都有漏洞，尝试找到它
2. **边界测试**: 输入极端值（空值、超大值、特殊字符）
3. **并发模拟**: 同时执行多个操作，观察竞态条件
4. **链路追踪**: 从用户操作追溯到数据库，检查每个环节
5. **权限绕过**: 尝试通过 URL 参数、API 直接调用绕过前端限制

### 工具推荐

- **浏览器 DevTools**: 检查 LocalStorage、Network、Console
- **Postman/Insomnia**: 测试 API 接口和参数校验
- **Burp Suite**: 抓包分析前后端通信
- **Chrome Lighthouse**: 性能和最佳实践检查
- **ESLint + SonarQube**: 代码质量和安全问题扫描

---

## ⚠️ 注意事项

### 必须遵守的原则

✅ **完整性优先**: 宁可花更多时间，也要保证审计深度  
✅ **证据充分**: 每个问题都要有代码行号和复现步骤  
✅ **修复可行**: 提供的修复方案必须可落地验证  
✅ **不改变功能**: 修复不能影响原有业务逻辑  

### 常见误区

❌ 只检查表面问题，忽略深层逻辑  
❌ 凭经验猜测，不查看实际代码  
❌ 以"篇幅过长"为由省略内容  
❌ 修复时引入新的问题  
❌ 忽略边界条件和异常场景  

---

## 📞 获取帮助

- 📖 完整文档: [SKILL.md](SKILL.md)
- 📝 更新说明: [VERSION_2.0_UPDATE.md](VERSION_2.0_UPDATE.md)
- 💬 问题反馈: GitHub Issues

---

**最后更新**: 2026-05-19  
**版本**: v2.0  
**作者**: by_皓月
